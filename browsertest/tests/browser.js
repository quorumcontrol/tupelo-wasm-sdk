import { expect } from 'chai'
import { Community } from 'tupelo-wasm-sdk'

describe("browser", ()=> {
    it("hello worlds", ()=> {
        expect(false).to.be.true
    })

    // it('communities', ()=> {
    //     const c = Community.freshLocalTestCommunity()
    //     expect(c).to.not.be.null
    // })
})